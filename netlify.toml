[build]
  functions = "netlify/functions"
  publish = "dist"
  command = "npm run build"

[dev]
  framework = "vite"
  targetPort = 5173
  port = 8888
  command = "npm run dev"
  envFile = ".env.local"
  autoLaunch = false
